const test = require('./7'),
    expect = require('chai').expect
describe('Exercice 7', () => {

    it('La recherche dans le tableau devrait renvoyer true', () => {
        expect(test()).to.be.true;
    });
})