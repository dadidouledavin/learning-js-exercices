// Le matchmaking d'overwatch est en trainn de se faire
// Deux équipe de trois dans un match de 6v6 sont en attentes
// Combinez ces deux équipes pour n'en faire qu'une seule
// Pour vérifier vos résultats lancez la commande "npm run test:1" dans votre terminal

function test() {
  const equipeEnAttente = ['macree', 'hanzo', 'reinhardt'],
    herosARajouter = ['mercy', 'mei', 'tracer'];

  return [];

  // Le résultat doit être de la manière suivante : ['macree', 'hanzo', 'reindhardt', 'mercy', 'mei', 'tracer']
  // Envoyez votre résultat en faisant "return <variableContenantLeRésultat >"
}

module.exports = test;
