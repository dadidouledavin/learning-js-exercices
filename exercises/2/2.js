// Nous avons récupéré le nom des champions présent du coté bleu de la faille
// Essayez de créer une String avec tous les champions séparés par un espace
// Pour vérifier vos résultats lancez la commande "npm run test:2" dans votre terminal

function test() {
  const champions = ['shen', 'nidalee', 'ryze', 'sivir', 'alistar'];

  // Le résultat doit être de la manière suivante : 'shen nidalee ryze sivir alistar'
  // Envoyez votre résultat en faisant "return <variableContenantLeRésultat >"
}

module.exports = test;
