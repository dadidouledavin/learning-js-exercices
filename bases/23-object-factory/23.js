// Pour vérifier vos résultats lancez la commande "npm run test:base:23" dans le terminal

function test() {
	// Définir une fonction qui créée un objet à partir des valeurs qui lui sont passées
	// L'objet devrait avoir deux propriété : "valeur" et "nom"
	// retournez l'objet retourné par la fonction pour le vérifier
	// Il devrait correspondre au contenu de l'objet ci-dessous
	// { valeur: 3, nom: "hephaistos" }

	const valeur = 3;
	const nom = 'hephaistos';

	// votre code ici
}

module.exports = test;
