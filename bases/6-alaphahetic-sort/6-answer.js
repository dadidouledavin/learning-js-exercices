// Pour vérifier vos résultats lancez la commande "npm run test:base:3" dans le terminal

function test() {
	// trier par ordre alphabetique
	// votre programme devrait afficher ['a', 'b', 'c', 'd'] dans la console
	const tableau = ['c', 'd', 'a', 'b'];

	// votre code ici
	return tableau.sort();
}

module.exports = test;
