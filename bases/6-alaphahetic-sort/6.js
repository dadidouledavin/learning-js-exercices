// Pour vérifier vos résultats lancez la commande "npm run test:base:6" dans le terminal

function test() {
	// trier par ordre alphabetique
	// votre programme devrait retourner ['a', 'b', 'c', 'd']
	const tableau = ['c', 'd', 'a', 'b'];

	// votre code ici
}

module.exports = test;
