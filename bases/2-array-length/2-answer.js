// Pour vérifier vos résultats lancez la commande "npm run test:base:2" dans le terminal

function test() {
  // // Récupérer la longeur totale du tableau
  // votre programme devrait afficher 2 dans la console
  const tableau = ['Hello world', 'this is javascript'];

  return tableau.length;

  // votre code ici
}

module.exports = test;
