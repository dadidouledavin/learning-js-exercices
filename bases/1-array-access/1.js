// Pour vérifier vos résultats lancez la commande "npm run test:base:1" dans le terminal

function test() {
  // Récupérer la 1ère valeur du tableau
  // votre programme devrait retourner "hello world"
  const tableau = ['Hello world', 'this is javascript'];

  // votre code ici
  return tableau[0];
}

module.exports = test;
