// Pour vérifier vos résultats lancez la commande "npm run test:base:3" dans le terminal

function test() {
	// Trouver la position dans le tableau de l'élément 'Severus'
	// votre programme devrait afficher 3 dans la console
	const tableau = ['Hermione', 'Harry', 'Ron', 'Severus'];

	// votre code ici
	return tableau.indexOf('Severus');
}

module.exports = test;
