// Pour vérifier vos résultats lancez la commande "npm run test:base:4" dans le terminal

function test() {
	// Trier les éléments par ordre de grandeur (plus petit au plus grand)
	// votre programme devrait retourner [1,2,3,4,5]
	const tableau = [2, 4, 5, 3, 1];

	// votre code ici
}

module.exports = test;
